# Z-News Monorepo

Merged from three previously separate repositories, each preserved with full commit history under `apps/`:

- `apps/server` — z-news-server
- `apps/adminpanel` — z-news-adminpanel
- `apps/website` — z-news-website-next.js
